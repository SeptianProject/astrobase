---
const { title, content } = Astro.props;
---

<div
  class="border bg-transparent border-white/20
  w-56 h-40 rounded-lg overflow-hidden transition-all duration-300
  group hover:scale-105 hover:-translate-y-2"
>
  <div class="flex flex-col items-center justify-end gap-y-5 size-full">
    <div class="text-center text-white">
      <h2 class="text-2xl font-semibold">{title}</h2>
      <p class="text-lg font-light">{content}</p>
    </div>
    <button
      type="button"
      class="bg-secondary text-white w-full py-3 text-base
      transition-all duration-500 hover:bg-secondary/80"
    >
      Show More
    </button>
  </div>
</div>
